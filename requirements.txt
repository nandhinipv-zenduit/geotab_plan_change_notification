# Runtime dependencies for the Geotab active-device-plan change alert.
#
# Upper bounds are deliberate: this runs unattended on a 3-hour schedule, and
# a surprise major version of requests on some future Tuesday should not be
# able to break a job nobody is watching. Bump them on purpose, not by luck.

requests>=2.31,<3.0
python-dotenv>=1.0,<2.0

# Nothing else is needed. csv, gzip, smtplib, email and argparse are all in the
# Python standard library, so there is no pandas here for what is a three-column
# dict comparison — dropping that dependency keeps cold-start installs to a
# couple of seconds on a GitHub runner.
#
# Requires Python 3.9+ (uses dict[str, dict] / str | None annotations, guarded
# by `from __future__ import annotations`).
